import WallCalendar from "@/components/WallCalendar";

const Index = () => {
  return (
    <main className="min-h-[100dvh] bg-background flex items-start justify-center">
      <WallCalendar />
    </main>
  );
};

export default Index;
